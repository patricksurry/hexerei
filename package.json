{
  "name": "hexerei",
  "private": true,
  "description": "Monorepo for HexMap tools",
  "workspaces": [
    "core",
    "renderer",
    "canvas",
    "editor"
  ],
  "scripts": {
    "test": "vitest run",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^20.11.10",
    "jsdom": "^24.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.6.1"
  },
  "dependencies": {
    "ajv-cli": "^5.0.0"
  }
}
