# Project Purpose: HexMap RFC

The HexMap RFC project aims to define a standardized, modular format for representing hexagonal grid maps, primarily for wargames. It provides a human-readable (YAML) and machine-parseable (JSON) specification for hex grids, terrain types, and map features. The documentation itself is built using a modular workflow with `mmark` for RFC-style formatting.
