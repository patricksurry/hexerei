<?xml version="1.0" encoding="utf-8"?>
<!-- name="GENERATOR" content="github.com/mmarkdown/mmark Mmark Markdown Processor - mmark.miek.nl" -->
<rfc version="3" ipr="none" docName="hexmap-v1.0-rev3" submissionType="independent" category="info" xml:lang="en" xmlns:xi="http://www.w3.org/2001/XInclude" indexInclude="true">

<front>
<title abbrev="HexMap">The HexMap Format</title><seriesInfo value="hexmap-v1.0-rev3" stream="independent" status="informational" name="Draft"></seriesInfo>
<author initials="P.D." surname="Surry" fullname="P.D. Surry"><organization>Hexerei Project</organization><address><postal><street></street>
</postal><email>hexerei@example.com</email>
</address></author><date/>
<area>Internet</area>
<workgroup>Hexerei Project</workgroup>
<keyword>hexmap</keyword>
<keyword>wargames</keyword>
<keyword>hex grid</keyword>

<abstract>
<t>This document describes the HexMap format.</t>
</abstract>

</front>

<middle>

<section anchor="the-hexmap-format"><name>The HexMap Format</name>
<t><strong>hexerei project — format specification draft</strong>
<strong>February 2026, revision 3</strong></t>

<section anchor="status-of-this-document"><name>Status of This Document</name>
<t>This is an early draft of the HexMap interchange format, written in the style
of an IETF RFC for precision and clarity. It is not an Internet Standard.
The key words &quot;MUST&quot;, &quot;SHOULD&quot;, &quot;MAY&quot;, and &quot;OPTIONAL&quot; are used as described
in RFC 2119.</t>
</section>

<section anchor="abstract-1"><name>Abstract</name>
<t>This document defines HexMap, a format for representing hexagonal grid maps
used in wargames and similar board games. The format captures grid geometry,
terrain, edge features, paths, and metadata in a human-readable structure
with a formal schema for validation. It is designed for interchange between
map editors, game engines, renderers, and AI systems.</t>
<t>The canonical serialization is JSON. YAML is defined as an equivalent
authoring-friendly serialization. A JSON Schema is provided for validation
in both cases.</t>
<t>PDS: should yaml be the canonical version to encourage hand-authoring?</t>
</section>

<section anchor="introduction"><name>Introduction</name>
<t>Hex grids have been used in wargames since 1961 (Avalon Hill's <em>Gettysburg</em>
second edition). Despite six decades of use, no standard interchange format
exists for hex map data. Maps are stored in proprietary tool-specific formats
(Worldographer XML, VASSAL modules), as images with no semantic data, or as
ad-hoc CSV/JSON with no schema.</t>
<t>The HexMap format fills this gap. Its design goals are:</t>

<ol>
<li><t><strong>Human-readable and hand-editable.</strong> A designer should be able to author
a small map in a text editor without tooling.</t>
</li>
<li><t><strong>Machine-validatable.</strong> A JSON Schema enables automated validation in
any language with a JSON Schema library.</t>
</li>
<li><t><strong>Language-neutral.</strong> The format is defined in terms of JSON data types
and is trivially consumable from Python, JavaScript/TypeScript, and any
language with a JSON parser.</t>
</li>
<li><t><strong>Game-neutral.</strong> The format describes map <em>structure</em> (where terrain is,
what edges have rivers) but does not prescribe game <em>mechanics</em> (movement
costs, combat modifiers). Terrain types are an extensible vocabulary
defined per map, not a fixed enum.</t>
</li>
<li><t><strong>Graph-duality aware.</strong> Hex grids have a natural planar graph structure
where faces (hexes), edges (hexsides), and vertices each carry game-
relevant information. Rivers run along edges, roads pass through hex
centers, bridges sit at vertices. The format treats all three as first-
class citizens.</t>
</li>
</ol>

<section anchor="relationship-to-geojson"><name>Relationship to GeoJSON</name>
<t>HexMap is not a profile of GeoJSON. While GeoJSON is excellent for geographic
data, it is poorly suited to hex maps: it requires 6-vertex polygons per hex
(verbose), has no concept of adjacency or edges, and its coordinate system
is lat/lng. HexMap uses a purpose-built coordinate system with compact hex
addressing.</t>
<t>Conversion from HexMap to GeoJSON is straightforward and SHOULD be supported
by implementations. A hex map with a defined geographic projection can be
losslessly converted to a GeoJSON feature collection.</t>
<t>Conversion from GeoJSON to HexMap is not generally lossless and requires
heuristics (identifying the hex grid, snapping features to hexes, etc.).
Implementations MAY support this as an import/digitization aid.</t>
</section>
</section>

<section anchor="conventions-and-terminology"><name>Conventions and Terminology</name>
<t><strong>hex</strong>: A single hexagonal cell on the grid. Synonyms: cell, face.</t>
<t><strong>edge</strong>: A side of a hex. Each interior edge is shared by exactly two
adjacent hexes. Boundary edges belong to only one hex. Synonym: hexside.</t>
<t><strong>half-edge</strong>: One side of an edge, belonging to a specific hex. Each
interior edge has two half-edges (one for each adjacent hex). Half-edges
are important for directed features like cliffs, where the effect differs
depending on which side you are on. In the addressing notation <tt>A13/NE</tt>,
the half-edge implicitly belongs to hex A13.</t>
<t><strong>vertex</strong>: A corner point where three hexes meet (or fewer at boundaries).</t>
<t><strong>face</strong>: Synonym for hex, used when emphasizing the graph-duality
relationship between faces, edges, and vertices.</t>
<t><strong>flat-top</strong>: A hex orientation where the top and bottom sides are
horizontal (flat edges at 12 o'clock and 6 o'clock). The hex is wider
than it is tall.</t>
<t><strong>pointy-top</strong>: A hex orientation where vertices point up and down
(vertices at 12 o'clock and 6 o'clock). The hex is taller than it is wide.</t>
<t><strong>cube coordinates</strong>: A three-integer coordinate system (u, v, w) for hex
grids where u + v + w = 0. This is the canonical mathematical
representation for hex math (distance, neighbors, line drawing, rings).
Two coordinates suffice since the third is determined by the constraint;
the two-component form (q, r) is called <strong>axial coordinates</strong>.</t>
<t>The letters u, v, w are used (rather than x, y, z) to avoid confusion
with screen coordinates or row/column indices.</t>
<t><strong>user coordinates</strong>: A human-readable labeling scheme for hexes, such as
<tt>&quot;0304&quot;</tt> (column 03, row 04) or <tt>&quot;C4&quot;</tt> (column C, row 4). Defined per map.
User coordinates are the labels that appear in the HexMap file.</t>
<t><strong>terrain type</strong>: A symbolic identifier for a category of terrain (e.g.,
<tt>forest</tt>, <tt>river</tt>). Defined per map in the terrain vocabulary.</t>
<t><strong>feature</strong>: An entry in the features list that associates a geometric
selection (hexes, edges, vertices, or a path) with terrain and properties.</t>
<t><strong>path</strong>: An ordered sequence of connected hexes or edges representing a
linear feature such as a road, railroad, or river.</t>
<t><strong>directed edge type</strong>: An edge terrain type where the effect is
asymmetric — different depending on which side you approach from (e.g.,
cliff, slope). The referencing hex in the addressing notation indicates
the &quot;active&quot; side. The semantic meaning of &quot;active&quot; (e.g., whether it
is the base or top of a cliff) is determined by the game, not this format.</t>
</section>

<section anchor="format-overview"><name>Format Overview</name>
<t>A HexMap document is a single JSON object (or equivalent YAML document)
with the following top-level structure:</t>
<t>hexmap: &quot;1.0&quot;              # format version (REQUIRED)
metadata: { ... }          # map identity and descriptive info
grid: { ... }              # hex geometry and coordinate system
terrain: { ... }           # terrain type vocabulary (hex, edge, vertex)
defaults: { ... }          # default values for hexes, edges, vertices
features: [ ... ]          # the map content: terrain, paths, regions
extensions: { ... }        # implementation-specific extensions</t>
<t>Only <tt>hexmap</tt> and <tt>grid</tt> are REQUIRED. All other sections are OPTIONAL
and default to empty.</t>
<t>The design is intentionally flat at the top level: six sections, each
with a clear role. The <tt>features</tt> list is the heart of the document —
it carries all map content as an ordered sequence of feature entries.</t>
</section>

<section anchor="data-model"><name>Data Model</name>

<section anchor="document-envelope"><name>Document Envelope</name>
<table>
<thead>
<tr>
<th>Field</th>
<th>Type</th>
<th>Required</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>hexmap</tt></td>
<td>string</td>
<td>YES</td>
<td>Format version. MUST be <tt>&quot;1.0&quot;</tt> for this spec.</td>
</tr>

<tr>
<td><tt>metadata</tt></td>
<td>object</td>
<td>no</td>
<td>Descriptive metadata (Section 4.2).</td>
</tr>

<tr>
<td><tt>grid</tt></td>
<td>object</td>
<td>YES</td>
<td>Grid geometry and coordinates (Section 4.3).</td>
</tr>

<tr>
<td><tt>terrain</tt></td>
<td>object</td>
<td>no</td>
<td>Terrain type definitions (Section 4.5).</td>
</tr>

<tr>
<td><tt>defaults</tt></td>
<td>object</td>
<td>no</td>
<td>Default hex/edge/vertex properties (Section 4.6).</td>
</tr>

<tr>
<td><tt>features</tt></td>
<td>array</td>
<td>no</td>
<td>Map content (Section 4.7).</td>
</tr>

<tr>
<td><tt>extensions</tt></td>
<td>object</td>
<td>no</td>
<td>Reserved for extensions (Section 8).</td>
</tr>
</tbody>
</table></section>

<section anchor="metadata"><name>Metadata</name>
<t>The <tt>metadata</tt> object carries descriptive information about the map.
All fields are OPTIONAL.</t>
<t>metadata:
  id: battle-for-moscow
  version: &quot;1.0&quot;
  title: &quot;Battle for Moscow&quot;
  description: &quot;The German advance on Moscow, Oct-Dec 1941&quot;
  designer: &quot;Frank Chadwick&quot;
  publisher: &quot;GDW&quot;
  date: &quot;1986&quot;
  source:
    url: &quot;<eref target="https://grognard.com/bfm/game.html&quot;">https://grognard.com/bfm/game.html"</eref>
    notes: &quot;Freely available introductory wargame&quot;</t>
<table>
<thead>
<tr>
<th>Field</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>id</tt></td>
<td>string</td>
<td>Machine-readable identifier. SHOULD be lowercase with hyphens.</td>
</tr>

<tr>
<td><tt>version</tt></td>
<td>string</td>
<td>Version of this map definition (for errata, revisions).</td>
</tr>

<tr>
<td><tt>title</tt></td>
<td>string</td>
<td>Human-readable name.</td>
</tr>

<tr>
<td><tt>description</tt></td>
<td>string</td>
<td>Longer description.</td>
</tr>

<tr>
<td><tt>designer</tt></td>
<td>string</td>
<td>Map or game designer.</td>
</tr>

<tr>
<td><tt>publisher</tt></td>
<td>string</td>
<td>Publisher name.</td>
</tr>

<tr>
<td><tt>date</tt></td>
<td>string</td>
<td>Publication date or year.</td>
</tr>

<tr>
<td><tt>source</tt></td>
<td>object</td>
<td>Provenance information.</td>
</tr>
</tbody>
</table><t>PDS: why is source a nested object, what is notes v description?
check pyproject metadata to see if any other obvious optional fields like license, keywords?</t>
<t>The <tt>version</tt> field tracks revisions of the map data itself (not the
format version, which is in the top-level <tt>hexmap</tt> field). Use semantic
versioning or simple incrementing numbers.</t>
<t>Implementations MUST preserve unrecognized fields in <tt>metadata</tt> and pass
them through unchanged.</t>
</section>

<section anchor="grid-geometry"><name>Grid Geometry</name>
<t>The <tt>grid</tt> object defines the hex grid's physical geometry and shape.
It is REQUIRED.</t>
<t>grid:
  hex_top: flat
  columns: 22
  rows: 15
  stagger: low
  coordinates:
    label: XXYY
    origin: top-left
    first: [1, 1]
  geo:
    scale: 16000              # meters per hex
    anchor: [54.5, 35.0]      # lat, lng of hex 0101 center
    bearing: 0                 # degrees, 0 = north up
    projection: mercator</t>
<t>PDS: what about stagger taking values like &quot;&lt;&quot; and &quot;&gt;&quot;?  Where &quot;&lt;&quot; means that the first
row(column) is offset towards increasing column(row) indices, and &quot;&gt;&quot; vice versa.
Is that problematic for YAML?  It's visually 'readable' and is clear about how
indices increasing LR/TB vs RL/BT would work.</t>
<t>PDS: alternative, use offset: indent vs hanging or some variation, since that's
a familiar concept for first line of text vs next</t>
<t>PDS: we need to explain a bit about how row, col relates to p, q (are they the same?)
and u,v,w in an informal way (maybe a diagram) early on, and then refer to details later.</t>
<t>PDS: for labeling, might there also be schemes that label along two of the u/v/w axes
rather than just rows/columns?  Maybe labeling scheme based on axis letters (repeated
if padded) with lower case meaning numeric and upper meaning letters?   e.g. Prrr
would use P as letters, 3-digit r as number?</t>
<table>
<thead>
<tr>
<th>Field</th>
<th>Type</th>
<th>Required</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>hex_top</tt></td>
<td>string</td>
<td>YES</td>
<td><tt>&quot;flat&quot;</tt> or <tt>&quot;pointy&quot;</tt>.</td>
</tr>

<tr>
<td><tt>columns</tt></td>
<td>integer</td>
<td>YES</td>
<td>Number of columns.</td>
</tr>

<tr>
<td><tt>rows</tt></td>
<td>integer</td>
<td>YES</td>
<td>Number of rows.</td>
</tr>

<tr>
<td><tt>stagger</tt></td>
<td>string</td>
<td>no</td>
<td><tt>&quot;low&quot;</tt> or <tt>&quot;high&quot;</tt>. Default: <tt>&quot;low&quot;</tt>. See below.</td>
</tr>

<tr>
<td><tt>boundary</tt></td>
<td>array</td>
<td>no</td>
<td>Hex IDs of valid hexes for non-rectangular maps.</td>
</tr>

<tr>
<td><tt>coordinates</tt></td>
<td>object</td>
<td>no</td>
<td>User coordinate labeling. See Section 4.4.</td>
</tr>

<tr>
<td><tt>geo</tt></td>
<td>object</td>
<td>no</td>
<td>Geographic scale and anchoring. See below.</td>
</tr>
</tbody>
</table><t>The <tt>columns</tt> and <tt>rows</tt> fields are always REQUIRED. They define the
bounding rectangle and coordinate space for the map. For non-rectangular
maps, all hexes within the bounding rectangle exist by default (if a
default terrain is set in Section 4.6). The optional <tt>boundary</tt> field
restricts which hexes are valid — hexes outside the boundary but within
the bounding rectangle do not exist. If <tt>boundary</tt> is absent and no
default terrain is set, only hexes explicitly mentioned in features exist.</t>

<section anchor="hex-top"><name>hex_top</name>
<t>Two values: <tt>&quot;flat&quot;</tt> (flat edge at 12 o'clock) or <tt>&quot;pointy&quot;</tt> (vertex at
12 o'clock). These are the standard terms used across the hex grid
literature.</t>
</section>

<section anchor="stagger"><name>stagger</name>
<t>When hex columns (flat-top) or rows (pointy-top) are arranged in a
rectangular grid, alternating columns/rows must be offset. The <tt>stagger</tt>
field specifies which ones are shifted.</t>
<table>
<thead>
<tr>
<th>Value</th>
<th>Flat-top meaning</th>
<th>Pointy-top meaning</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>&quot;low&quot;</tt> (default)</td>
<td>Odd columns sit lower</td>
<td>Odd rows sit further right</td>
</tr>

<tr>
<td><tt>&quot;high&quot;</tt></td>
<td>Odd columns sit higher</td>
<td>Odd rows sit further left</td>
</tr>
</tbody>
</table><t>&quot;Odd&quot; columns/rows are those whose index (after applying <tt>first</tt>) is odd:
columns 1, 3, 5, ... when <tt>first</tt> starts at 1. <tt>&quot;low&quot;</tt> corresponds to
Red Blob Games' &quot;odd-q&quot; (flat-top) or &quot;odd-r&quot; (pointy-top).</t>
<t>The following diagrams show the vertical offset of hex centers for flat-top
grids. Each label shows the hex user coordinate (XXYY format, first: [1,1]):</t>

<artwork><![CDATA[stagger: low                     stagger: high
(odd columns sit lower)          (odd columns sit higher)

   c1     c2     c3                c1     c2     c3

         0201                    0101          0301
  0101          0301                    0201
         0202                    0102          0302
  0102          0302                    0202
         0203                    0103          0303
  0103          0303                    0203
]]></artwork>
<t>For pointy-top grids, the pattern is analogous but applied to rows
instead of columns, with the offset being horizontal rather than vertical.</t>
</section>

<section anchor="geo-geographic-anchoring-and-scale"><name>geo (geographic anchoring and scale)</name>
<t>The optional <tt>geo</tt> object provides physical scale and anchors the hex
grid to real-world geography.</t>
<table>
<thead>
<tr>
<th>Field</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>scale</tt></td>
<td>number</td>
<td>Meters across a hex (center to center of opposite edges).</td>
</tr>

<tr>
<td><tt>anchor</tt></td>
<td>[number, number]</td>
<td>[latitude, longitude] of a reference hex center.</td>
</tr>

<tr>
<td><tt>anchor_hex</tt></td>
<td>string</td>
<td>Which hex the anchor refers to. Default: first hex.</td>
</tr>

<tr>
<td><tt>bearing</tt></td>
<td>number</td>
<td>Rotation in degrees clockwise from north. Default: 0.</td>
</tr>

<tr>
<td><tt>projection</tt></td>
<td>string</td>
<td>Map projection identifier. Default: <tt>&quot;mercator&quot;</tt>.</td>
</tr>
</tbody>
</table><t>PDS: for anchor lng,lat is more typical no? or make it an object with latitude, longitude, hex for clarity</t>
<t>PDS: is bearing the angle between top and the compass north on the map, or the bearing of up? should we rename eg. top_bearing for clarity?</t>
<t>Only <tt>scale</tt> is needed for maps that specify physical size without geographic
positioning. The full combination of anchor, scale, bearing, and projection
defines the hex-to-geography mapping for real-world overlay.</t>
</section>
</section>

<section anchor="coordinate-systems"><name>Coordinate Systems</name>
<t>The HexMap format uses two coordinate representations:</t>

<ol>
<li><t><strong>User coordinates</strong> (strings): Human-readable labels like <tt>&quot;0304&quot;</tt> or
<tt>&quot;C4&quot;</tt>. These appear in the file as hex identifiers in features, edges,
paths, etc. They MUST be unique within a map.</t>
</li>
<li><t><strong>Cube coordinates</strong> (u, v, w integers): The canonical mathematical
representation where u + v + w = 0. Implementations SHOULD use cube
coordinates internally for hex math (distance, neighbors, rings, line
drawing). Cube coordinates do not appear in the file.</t>
</li>
</ol>
<t>The mapping between user coordinates and cube coordinates is determined
by the grid geometry (hex_top, stagger) and the coordinate label format.</t>

<section anchor="user-coordinate-label-format"><name>User coordinate label format</name>
<t>The <tt>coordinates</tt> object defines the labeling scheme:</t>

<sourcecode type="yaml"><![CDATA[coordinates:
  label: XXYY
  origin: top-left
  first: [1, 1]
]]></sourcecode>
<table>
<thead>
<tr>
<th>Field</th>
<th>Type</th>
<th>Default</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>label</tt></td>
<td>string</td>
<td><tt>&quot;XXYY&quot;</tt></td>
<td>Label pattern (see below).</td>
</tr>

<tr>
<td><tt>origin</tt></td>
<td>string</td>
<td><tt>&quot;top-left&quot;</tt></td>
<td>Corner where numbering starts (visual direction).</td>
</tr>

<tr>
<td><tt>first</tt></td>
<td>[int, int]</td>
<td><tt>[1, 1]</tt></td>
<td>Starting [column, row] number at the origin.</td>
</tr>
</tbody>
</table><t><strong><tt>origin</tt></strong> specifies the visual corner of the map where the lowest-
numbered hex appears — it controls the <em>direction</em> numbers increase.
<strong><tt>first</tt></strong> specifies what number to assign that corner hex — it controls
the <em>starting value</em>. Together: <tt>origin: top-left</tt> with <tt>first: [1, 1]</tt>
means hex (1,1) is at the top-left, columns increase rightward, rows
increase downward. Changing <tt>first: [0, 0]</tt> makes that same corner hex
(0,0) instead, but doesn't change the visual layout.</t>
<t><strong>Label patterns</strong> use <tt>X</tt> for column digits, <tt>Y</tt> for row digits, and
<tt>A</tt> for column letters. The number of repeated characters indicates
zero-padded width. Any other characters are literal punctuation.</t>
<table>
<thead>
<tr>
<th>Pattern</th>
<th>Example</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>XXYY</tt></td>
<td><tt>&quot;0304&quot;</tt></td>
<td>2-digit column + 2-digit row (most common)</td>
</tr>

<tr>
<td><tt>XXXYY</tt></td>
<td><tt>&quot;00304&quot;</tt></td>
<td>3-digit column + 2-digit row</td>
</tr>

<tr>
<td><tt>AYY</tt></td>
<td><tt>&quot;C04&quot;</tt></td>
<td>Letter column + 2-digit row</td>
</tr>

<tr>
<td><tt>AY</tt></td>
<td><tt>&quot;C4&quot;</tt></td>
<td>Letter column + unpadded row</td>
</tr>

<tr>
<td><tt>(X,Y)</tt></td>
<td><tt>&quot;(3,4)&quot;</tt></td>
<td>Parenthesized, comma-separated</td>
</tr>

<tr>
<td><tt>XX.YY</tt></td>
<td><tt>&quot;03.04&quot;</tt></td>
<td>Dot-separated</td>
</tr>
</tbody>
</table><t>These patterns cover the most common wargame labeling schemes. The set is
intentionally minimal for v1.0. Future versions may add support for more
complex schemes (e.g., board-prefixed notation like <tt>&quot;3-1204&quot;</tt> for
geomorphic multi-board maps). For unusual labeling needs, the <tt>properties</tt>
field on features can carry alternative identifiers.</t>
<t><strong>Origin</strong> specifies the corner where numbering begins:</t>
<table>
<thead>
<tr>
<th>Value</th>
<th>Column numbers increase</th>
<th>Row numbers increase</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>&quot;top-left&quot;</tt> (default)</td>
<td>left to right</td>
<td>top to bottom</td>
</tr>

<tr>
<td><tt>&quot;bottom-left&quot;</tt></td>
<td>left to right</td>
<td>bottom to top</td>
</tr>

<tr>
<td><tt>&quot;top-right&quot;</tt></td>
<td>right to left</td>
<td>top to bottom</td>
</tr>

<tr>
<td><tt>&quot;bottom-right&quot;</tt></td>
<td>right to left</td>
<td>bottom to top</td>
</tr>
</tbody>
</table><t>Most wargames use <tt>&quot;top-left&quot;</tt> (XXYY with 01,01 in the upper-left).</t>
</section>
</section>

<section anchor="terrain-vocabulary"><name>Terrain Vocabulary</name>
<t>The <tt>terrain</tt> object defines all terrain types used on this map, organized
by the geometry they apply to: hexes, edges, vertices, or paths.</t>
<t>PDS: solve for 'mutually exclusive' terrain, for ease of stencilling layers
and overriding.  could use simple primary/secondary switch, or support
sets (lists?) of terrains that are mutually exclusive.  could support
optional constraints on terrain (requires, implies, excludes each listing names)
which would allow richer validation but maybe that's overkill/or an extension?</t>
<t>terrain:
  hex:
    clear:
      name: &quot;Clear&quot;
    forest: {}
    swamp: {}
    city: {}
    major_city:
      name: &quot;Major City&quot;</t>
<t>edge:
    river: {}
    cliff:
      directed: true
    wall:
      name: &quot;Stone Wall&quot;
    slope:
      directed: true</t>
<t>vertex:
    bridge: {}
    crossroads: {}
    ford: {}</t>
<t>path:
    road: {}
    railroad: {}
    river:
      name: &quot;River&quot;</t>
<t>PDS: should include properties and display examples</t>

<section anchor="type-identifiers"><name>Type identifiers</name>
<t>Type identifiers MUST be lowercase ASCII with underscores for word
separation (e.g., <tt>light_woods</tt>, <tt>major_river</tt>).</t>
<t>When <tt>name</tt> is omitted, it defaults to the identifier converted from
snake_case to Title Case: <tt>light_woods</tt> -&gt; &quot;Light Woods&quot;,
<tt>major_river</tt> -&gt; &quot;Major River&quot;.</t>
</section>

<section anchor="terrain-type-definition-object"><name>Terrain type definition object</name>
<table>
<thead>
<tr>
<th>Field</th>
<th>Type</th>
<th>Required</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>name</tt></td>
<td>string</td>
<td>no</td>
<td>Human-readable display name. Auto-generated from identifier if omitted.</td>
</tr>

<tr>
<td><tt>directed</tt></td>
<td>boolean</td>
<td>no</td>
<td>Edge types only. If true, the feature is asymmetric (e.g., cliff). Default: false. Vertex types are never directed.</td>
</tr>

<tr>
<td><tt>style</tt></td>
<td>object</td>
<td>no</td>
<td>Display hints (color, pattern, etc). See below.</td>
</tr>

<tr>
<td><tt>properties</tt></td>
<td>object</td>
<td>no</td>
<td>Arbitrary additional properties for game-specific semantics.</td>
</tr>
</tbody>
</table><t>PDS: is 'directed' the right word here?  it's normally about which hex side rather than
which vertex end which is what directed implies.  perhaps onesided, or sides=one|both?</t>
<t>The <tt>properties</tt> field on terrain type definitions is intended for
gameplay-relevant data that the format itself does not interpret: movement
costs, combat modifiers, line-of-sight effects, etc. The format carries
this data opaquely. Example:</t>

<sourcecode type="yaml"><![CDATA[terrain:
  hex:
    forest:
      properties:
        movement_cost: 2
        defense_modifier: +1
        blocks_los: true
]]></sourcecode>
<t>The <tt>style</tt> object carries optional rendering hints:</t>
<table>
<thead>
<tr>
<th>Field</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>color</tt></td>
<td>string</td>
<td>Suggested fill color (CSS hex, e.g. <tt>&quot;#2d5a1e&quot;</tt>).</td>
</tr>

<tr>
<td><tt>pattern</tt></td>
<td>string</td>
<td>Suggested fill pattern name.</td>
</tr>

<tr>
<td><tt>stroke</tt></td>
<td>string</td>
<td>Suggested line color for edges/paths.</td>
</tr>

<tr>
<td><tt>stroke_width</tt></td>
<td>number</td>
<td>Suggested line width for edges/paths.</td>
</tr>
</tbody>
</table><t>Style is purely advisory. Renderers MAY ignore it entirely.</t>
</section>

<section anchor="geometry-subsections"><name>Geometry subsections</name>
<t>The four subsections — <tt>hex</tt>, <tt>edge</tt>, <tt>vertex</tt>, <tt>path</tt> — scope terrain
identifiers to their geometry. The same identifier MAY appear in multiple
subsections (e.g., <tt>river</tt> as both an edge type and a path type).</t>
<t><strong>When to use each subsection:</strong></t>

<ul>
<li><t><strong><tt>hex</tt></strong>: The feature fills an entire hex. Use for terrain that
occupies the cell: forests, cities, swamps, open water. At operational
scale, a wide river may be hex-filling terrain.</t>
</li>
<li><t><strong><tt>edge</tt></strong>: The feature is a property of a hexside. Use for linear
features that form boundaries between hexes: rivers at tactical scale,
cliffs, walls, hedges. Edge features carry game-mechanical meaning
(cost to cross, line-of-sight effects).</t>
</li>
<li><t><strong><tt>vertex</tt></strong>: The feature sits at a hex corner. Use for point features
where three hexes meet: bridges, fords, crossroads, hilltop
observation points. Vertex terrain types are never directed.</t>
</li>
<li><t><strong><tt>path</tt></strong>: The feature is a connected linear route spanning multiple
hexes or edges. Use for the continuous course of a named feature:
roads, railroads, rivers. Path features often duplicate edge features
(e.g., individual river hexsides as <tt>edge</tt> terrain for game mechanics,
plus the full river course as a <tt>path</tt> for display and identification).</t>
</li>
</ul>
<t>PDS: do we really need path as a separate thing?  use case is when
two hex-pathed rivers run parallel in adjacent hexes.  but couldn't
that just be two separate path-like regions? from a game perspective
it usually only matters that a hex contains a river or not, the connectivity
is a rendering concern which could be inferred from the region structure perhaps?</t>
<t>A feature like &quot;river&quot; may reasonably appear in all three of <tt>hex</tt>,
<tt>edge</tt>, and <tt>path</tt> depending on game scale and design. This is
intentional — the format does not prescribe which representation to use.</t>
<t>PDS: but hex:river, edge:river no implied relationship?</t>
<t><strong>The format does not define a fixed set of terrain types.</strong> Each map
declares its own vocabulary. This is intentional: terrain types are
game-specific, not universal. A format that baked in &quot;forest&quot; and &quot;swamp&quot;
would fail for science fiction, naval, or abstract games.</t>
<t>However, Appendix A provides a recommended conventional vocabulary for
common hex wargames as a non-normative interoperability aid.</t>
</section>
</section>

<section anchor="defaults"><name>Defaults</name>
<t>The <tt>defaults</tt> object specifies property values applied to every hex,
edge, or vertex that does not have an explicit value set in the
features list.</t>
<t>defaults:
  hex:
    terrain: clear
    elevation: 0
  edge: {}
  vertex: {}</t>
<t>PDS: elevation might be a bad example since it should probably default to 0 globally</t>
<table>
<thead>
<tr>
<th>Field</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>hex</tt></td>
<td>object</td>
<td>Default properties for all hexes.</td>
</tr>

<tr>
<td><tt>hex.terrain</tt></td>
<td>string</td>
<td>Default hex terrain type.</td>
</tr>

<tr>
<td><tt>hex.elevation</tt></td>
<td>integer</td>
<td>Default elevation level.</td>
</tr>

<tr>
<td><tt>edge</tt></td>
<td>object</td>
<td>Default properties for all edges (uncommon).</td>
</tr>

<tr>
<td><tt>vertex</tt></td>
<td>object</td>
<td>Default properties for all vertices (uncommon).</td>
</tr>
</tbody>
</table><t>Each property defaults independently. Setting <tt>defaults.hex.terrain</tt>
does not imply a default elevation; if <tt>defaults.hex.elevation</tt> is
also desired, it must be set explicitly. Similarly, a hex that appears
in the features list with only <tt>terrain</tt> set still inherits the default
<tt>elevation</tt> (if one is defined).</t>
<t>PDS: default elevation to zero</t>
<t>If <tt>defaults.hex.terrain</tt> is specified, all hexes within the grid bounds
(constrained by <tt>boundary</tt> if present) are assumed to exist with that
terrain. If no default terrain is specified, only hexes explicitly
mentioned in features exist. This provides a natural way to define
non-rectangular maps: omit the default terrain and enumerate only the
valid hexes in features.</t>
</section>

<section anchor="features"><name>Features</name>
<t>The <tt>features</tt> array is the heart of the document. It is an ordered list
of feature entries, each associating a geometric selection with terrain
and properties.</t>
<t>Each feature entry has a <strong>selector</strong> (which geometry it applies to) and
<strong>attributes</strong> (what properties to set). The selector is indicated by one
of the following keys:</t>
<table>
<thead>
<tr>
<th>Selector key</th>
<th>Geometry</th>
<th>Value type</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>hex</tt></td>
<td>single hex</td>
<td>string</td>
<td>One hex by user coordinate.</td>
</tr>

<tr>
<td><tt>hexes</tt></td>
<td>multiple hexes</td>
<td>array of strings</td>
<td>Multiple hexes by user coordinate.</td>
</tr>

<tr>
<td><tt>edge</tt></td>
<td>single edge</td>
<td>string</td>
<td>One edge in <tt>hex/direction</tt> notation.</td>
</tr>

<tr>
<td><tt>edges</tt></td>
<td>multiple edges</td>
<td>array of strings</td>
<td>Multiple edges.</td>
</tr>

<tr>
<td><tt>vertex</tt></td>
<td>single vertex</td>
<td>string</td>
<td>One vertex in <tt>hex.direction</tt> notation.</td>
</tr>

<tr>
<td><tt>vertices</tt></td>
<td>multiple vertices</td>
<td>array of strings</td>
<td>Multiple vertices.</td>
</tr>

<tr>
<td><tt>path</tt></td>
<td>hex path</td>
<td>array of strings</td>
<td>Ordered hex sequence (through-hex path).</td>
</tr>

<tr>
<td><tt>edge_path</tt></td>
<td>edge path</td>
<td>array of strings</td>
<td>Ordered edge sequence (along-edge path).</td>
</tr>

<tr>
<td><tt>region</tt></td>
<td>hex region</td>
<td>object</td>
<td>Named region of hexes.</td>
</tr>
</tbody>
</table><t>Exactly one selector key MUST be present per feature entry. The remaining
fields are attributes applied to the selected geometry.</t>
<t>PDS: &quot;... by user coordinate&quot; - should support backward references
to features by id; brainstorm as part of appendix C</t>

<section anchor="feature-attributes"><name>Feature attributes</name>
<table>
<thead>
<tr>
<th>Field</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>terrain</tt></td>
<td>string</td>
<td>Terrain type from the appropriate vocabulary subsection.</td>
</tr>

<tr>
<td><tt>elevation</tt></td>
<td>integer</td>
<td>Elevation level. Primarily used for hexes, but MAY also appear on edge or vertex features for games that require it (e.g., wall heights, bridge elevations).</td>
</tr>

<tr>
<td><tt>label</tt></td>
<td>string</td>
<td>Display name or label text.</td>
</tr>

<tr>
<td><tt>id</tt></td>
<td>string</td>
<td>Unique identifier for this feature (for cross-referencing).</td>
</tr>

<tr>
<td><tt>tags</tt></td>
<td>array of strings</td>
<td>Semantic tags (e.g., <tt>&quot;victory&quot;</tt>, <tt>&quot;fortified&quot;</tt>).</td>
</tr>

<tr>
<td><tt>properties</tt></td>
<td>object</td>
<td>Arbitrary key-value data.</td>
</tr>
</tbody>
</table></section>

<section anchor="feature-ordering-and-precedence"><name>Feature ordering and precedence</name>
<t>Features are applied in document order. When multiple features affect
the same hex, edge, or vertex, <strong>later entries override earlier ones</strong>
for scalar attributes (<tt>terrain</tt>, <tt>elevation</tt>, <tt>label</tt>). Array attributes
(<tt>tags</tt>) are merged (union). Object attributes (<tt>properties</tt>) are merged
with later keys overriding.</t>
<t>This enables a layered authoring style: set broad defaults first, then
override specific areas:</t>

<sourcecode type="yaml"><![CDATA[features:
  # Base terrain: everything is clear (or use defaults section)
  - hexes: ["0301", "0302", "0303", "0304", "0305",
            "0401", "0402", "0403", "0404", "0405"]
    terrain: forest

  # Override: one hex in the forest is actually a town
  - hex: "0403"
    terrain: town
    label: "Waldstadt"
]]></sourcecode>
<t><strong>Round-trip considerations.</strong> The format does not mandate round-trip
fidelity: an implementation that reads and writes a HexMap document MAY
produce a structurally different file (e.g., flattened features, reordered
entries) as long as the effective result is identical. Two documents are
semantically equivalent if, after applying defaults and feature
precedence, every hex, edge, and vertex has the same terrain, elevation,
tags, and properties. Implementations that need to preserve authoring
structure SHOULD treat the features list as opaque and pass it through.</t>
</section>

<section anchor="hex-features"><name>Hex features</name>

<sourcecode type="yaml"><![CDATA[features:
  - hex: "0507"
    terrain: major_city
    label: "Moscow"
    elevation: 1
    tags: [victory]
    properties:
      victory_points: 3

  - hexes: ["0302", "0303", "0402"]
    terrain: forest
]]></sourcecode>
</section>

<section anchor="edge-features"><name>Edge features</name>
<t>Edges are referenced using the addressing notation <tt>hex/direction</tt>
(see Section 5):</t>

<sourcecode type="yaml"><![CDATA[features:
  - edges: ["0304/N", "0304/NE", "0404/N"]
    terrain: river

  - edge: "0503/SE"
    terrain: cliff    # directed: 0503 is the "active" side
]]></sourcecode>
<t>For directed edge types (where <tt>directed: true</tt> in the terrain
vocabulary), the hex in the address indicates the &quot;active&quot; side.
The format does not prescribe the semantic meaning of &quot;active&quot; — that
is determined by the game. For example, a game might define <tt>cliff</tt>
such that the active side is the base (climbing up costs extra) or the
top (falling risk). The format provides the mechanism for asymmetric
annotation; the game provides the interpretation.</t>
<t>Both half-edges of an interior edge can be independently annotated.
If an edge has annotations from both adjacent hexes, both are valid
and coexist. This allows asymmetric features: e.g., a cliff that is
also a river.</t>
</section>

<section anchor="through-hex-paths"><name>Through-hex paths</name>
<t>A path that passes through hex centers, crossing the edge between each
consecutive pair. Used for roads, railroads, and any feature that
traverses hexes.</t>

<sourcecode type="yaml"><![CDATA[features:
  - path: ["0104", "0204", "0304", "0404", "0504"]
    terrain: road
    id: moscow-highway
    label: "Moscow Highway"
]]></sourcecode>
<t>Consecutive hexes in a path MUST be adjacent (sharing an edge). A path
with non-adjacent consecutive hexes is a validation error. The edges
crossed by the path are implicit — derived from the relative positions
of consecutive hexes.</t>
<t>A shorthand for straight-line paths between two hexes is deferred to the
geometry expression language (Appendix C, future).</t>
<t>PDS: is the consecutive hex/edge requirement in paths actually necessary?
does something break if we allow paths with gaps?  brainstorm as part of Appendix C.</t>
</section>

<section anchor="along-edge-paths"><name>Along-edge paths</name>
<t>A path that follows hex edges. Used for rivers, walls, and other
features that live on hexsides.</t>

<sourcecode type="yaml"><![CDATA[features:
  - edge_path: ["0306/NE", "0306/N", "0206/NE", "0206/N"]
    terrain: river
    id: nara-river
    label: "Nara River"
]]></sourcecode>
<t>Consecutive edges in an edge path MUST share a vertex (i.e., the path
must be connected). A disconnected sequence is a validation error. To
represent a feature with gaps, use separate edge_path entries.</t>
</section>

<section anchor="regions"><name>Regions</name>
<t>A named group of hexes. Regions can set terrain and tags on all
member hexes.</t>

<sourcecode type="yaml"><![CDATA[features:
  - region:
      id: soviet-fortifications
      hexes: ["0805", "0905", "1005", "1105"]
    tags: [fortified]
    label: "Soviet Fortification Line"

  - region:
      id: german-entry-west
      hexes: ["0101", "0102", "0103", "0104", "0105"]
    tags: [entry]
    label: "German Entry Area"
]]></sourcecode>
<table>
<thead>
<tr>
<th>Field</th>
<th>Type</th>
<th>Required</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>region.id</tt></td>
<td>string</td>
<td>no</td>
<td>Unique identifier for this region.</td>
</tr>

<tr>
<td><tt>region.hexes</tt></td>
<td>array</td>
<td>YES</td>
<td>List of hex user coordinates in this region.</td>
</tr>
</tbody>
</table><t>A shorthand for regions defined by a boundary (fill-from-edge) is
deferred to the geometry expression language (Appendix C, future).</t>
</section>

<section anchor="recommended-tags"><name>Recommended tags</name>
<t>Tags are freeform strings. The following are RECOMMENDED for conventional
use:</t>
<table>
<thead>
<tr>
<th>Tag</th>
<th>Meaning</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>victory</tt></td>
<td>Victory hex / objective.</td>
</tr>

<tr>
<td><tt>fortified</tt></td>
<td>Fortification or prepared defense.</td>
</tr>

<tr>
<td><tt>supply</tt></td>
<td>Supply source.</td>
</tr>

<tr>
<td><tt>entry</tt></td>
<td>Map entry hex for reinforcements.</td>
</tr>

<tr>
<td><tt>exit</tt></td>
<td>Map exit hex.</td>
</tr>
</tbody>
</table></section>
</section>
</section>

<section anchor="addressing-notation"><name>Addressing Notation</name>
<t>HexMap uses a compact notation for referencing hexes, edges, and vertices.
This notation appears in the <tt>features</tt> list and wherever geometry is
referenced.</t>

<section anchor="grammar"><name>Grammar</name>
<t>The addressing notation follows this grammar (ABNF-style):</t>

<artwork><![CDATA[hex-ref     = user-coord
edge-ref    = user-coord "/" direction
vertex-ref  = user-coord "." direction
direction   = compass-dir / index
compass-dir = "N" / "NE" / "E" / "SE" / "S" / "SW" / "W" / "NW"
index       = "1" / "2" / "3" / "4" / "5" / "6"
]]></artwork>
<t>PDS: user-coord | identifier (maybe identifier needs a prefix to avoid name clash?)</t>
<t>Where <tt>user-coord</tt> is a string matching the label pattern defined in
<tt>layout.coordinates</tt> (e.g., <tt>&quot;0304&quot;</tt> for XXYY).</t>
<t>The <tt>/</tt> separator marks edge references. The <tt>.</tt> separator marks vertex
references. Indices 1-6 are clockwise from 12 o'clock (see Section 5.5).</t>
</section>

<section anchor="hexes"><name>Hexes</name>
<t>A hex is referenced by its user coordinate string:</t>

<artwork><![CDATA[0304        # XXYY format
C4          # letter-number format
]]></artwork>
</section>

<section anchor="edges"><name>Edges</name>
<t>An edge is referenced as <tt>hex/direction</tt>:</t>

<artwork><![CDATA[0304/N      # north edge of hex 0304 (compass direction)
0304/1      # same edge, by clockwise index
]]></artwork>
</section>

<section anchor="vertices"><name>Vertices</name>
<t>A vertex is referenced as <tt>hex.direction</tt>:</t>

<artwork><![CDATA[0304.NE     # northeast vertex of hex 0304 (compass direction)
0304.1      # same vertex, by clockwise index
]]></artwork>
</section>

<section anchor="edge-and-vertex-directions"><name>Edge and vertex directions</name>
<t>The compass directions available for edges and vertices depend on
<tt>hex_top</tt>. The following diagrams show both on the same hex shape.
Edges are labeled along the sides; vertices are labeled at the corners:</t>
<t><strong>Flat-top</strong> (<tt>hex_top: flat</tt>):</t>

<artwork><![CDATA[               NW (v)    N (e)    NE (v)
                  · _____________ ·
                  /               \
      NW (e)    /                  \    NE (e)
               /                    \
          W (v)·                    · E (v)
               \                    /
      SW (e)    \                  /    SE (e)
                 \               /
                  · ____________·
               SW (v)    S (e)    SE (v)
]]></artwork>
<t>Edge directions: <strong>N, NE, SE, S, SW, NW</strong> (6 edges)
Vertex directions: <strong>NE, E, SE, SW, W, NW</strong> (6 vertices)</t>
<t><strong>Pointy-top</strong> (<tt>hex_top: pointy</tt>):</t>

<artwork><![CDATA[                       N (v)
                        /\
                       /  \
            NW (e)   /     \   NE (e)
                    /        \
           NW (v) ·           · NE (v)
                  |            |
            W (e) |            | E (e)
                  |            |
           SW (v) ·           · SE (v)
                    \        /
            SW (e)   \     /   SE (e)
                      \   /
                       \/
                       S (v)
]]></artwork>
<t>PDS: diagram is too squashed horizontally</t>
<t>Edge directions: <strong>NE, E, SE, SW, W, NW</strong> (6 edges)
Vertex directions: <strong>N, NE, SE, S, SW, NW</strong> (6 vertices)</t>
</section>

<section anchor="numeric-addressing-with-clock-hours"><name>Numeric addressing with clock hours</name>
<t>PDS: except they're not actually addressed with the clock hour,
or also allow a syntax like hex@hour?</t>
<t>Edges and vertices can also be addressed by clockwise index (1-6). The
following tables show the mapping, including the corresponding clock
hour position from the 12-position clock system (Appendix B):</t>
<t><strong>Flat-top:</strong></t>
<table>
<thead>
<tr>
<th>Index</th>
<th>Edge</th>
<th>Clock</th>
<th></th>
<th>Index</th>
<th>Vertex</th>
<th>Clock</th>
</tr>
</thead>

<tbody>
<tr>
<td>1</td>
<td>N</td>
<td>12</td>
<td></td>
<td>1</td>
<td>NE</td>
<td>1</td>
</tr>

<tr>
<td>2</td>
<td>NE</td>
<td>2</td>
<td></td>
<td>2</td>
<td>E</td>
<td>3</td>
</tr>

<tr>
<td>3</td>
<td>SE</td>
<td>4</td>
<td></td>
<td>3</td>
<td>SE</td>
<td>5</td>
</tr>

<tr>
<td>4</td>
<td>S</td>
<td>6</td>
<td></td>
<td>4</td>
<td>SW</td>
<td>7</td>
</tr>

<tr>
<td>5</td>
<td>SW</td>
<td>8</td>
<td></td>
<td>5</td>
<td>W</td>
<td>9</td>
</tr>

<tr>
<td>6</td>
<td>NW</td>
<td>10</td>
<td></td>
<td>6</td>
<td>NW</td>
<td>11</td>
</tr>
</tbody>
</table><t><strong>Pointy-top:</strong></t>
<table>
<thead>
<tr>
<th>Index</th>
<th>Edge</th>
<th>Clock</th>
<th></th>
<th>Index</th>
<th>Vertex</th>
<th>Clock</th>
</tr>
</thead>

<tbody>
<tr>
<td>1</td>
<td>NE</td>
<td>1</td>
<td></td>
<td>1</td>
<td>N</td>
<td>12</td>
</tr>

<tr>
<td>2</td>
<td>E</td>
<td>3</td>
<td></td>
<td>2</td>
<td>NE</td>
<td>2</td>
</tr>

<tr>
<td>3</td>
<td>SE</td>
<td>5</td>
<td></td>
<td>3</td>
<td>SE</td>
<td>4</td>
</tr>

<tr>
<td>4</td>
<td>SW</td>
<td>7</td>
<td></td>
<td>4</td>
<td>S</td>
<td>6</td>
</tr>

<tr>
<td>5</td>
<td>W</td>
<td>9</td>
<td></td>
<td>5</td>
<td>SW</td>
<td>8</td>
</tr>

<tr>
<td>6</td>
<td>NW</td>
<td>11</td>
<td></td>
<td>6</td>
<td>NW</td>
<td>10</td>
</tr>
</tbody>
</table><t>So <tt>0304/1</tt> is the first edge clockwise from 12 o'clock, and <tt>0304.1</tt>
is the first vertex. Both compass and numeric forms are valid.</t>
</section>

<section anchor="edge-and-vertex-equivalence"><name>Edge and vertex equivalence</name>
<t>Each interior edge is shared by two hexes, so the same physical edge can
be referenced from either side. For example, if hex <tt>0305</tt> is directly
north of hex <tt>0304</tt> (flat-top), then <tt>0304/N</tt> and <tt>0305/S</tt> refer to the
same edge.</t>
<t>Similarly, each interior vertex is shared by three hexes and can be
referenced from any of them.</t>
<t>Implementations MUST treat equivalent references as identical. A document
MAY reference the same edge or vertex from different hexes in different
feature entries. For undirected features, all references to the same
physical edge are equivalent. For directed features (<tt>directed: true</tt> in
the terrain vocabulary), the choice of referencing hex carries semantic
meaning (Section 4.7, Edge features).</t>
</section>
</section>

<section anchor="hex-geometry-reference"><name>Hex Geometry Reference</name>
<t>This section defines the mathematical relationships between coordinate
systems. It is normative: implementations MUST use these conversions.</t>

<section anchor="cube-coordinates"><name>Cube coordinates</name>
<t>Cube coordinates (u, v, w) satisfy the constraint <strong>u + v + w = 0</strong>.
This constraint means any hex can be identified by two coordinates; the
third is derived. The three axes are symmetric, which simplifies
algorithms for distance, neighbors, rings, and line drawing.</t>
<t>Axial coordinates (q, r) are a two-component shorthand:</t>

<artwork><![CDATA[q = u
r = w
v = -q - r
]]></artwork>
</section>

<section anchor="offset-to-cube-conversion"><name>Offset to cube conversion</name>
<t>User coordinates are parsed into (col, row) integers via the label
pattern, then converted to cube coordinates. The conversion depends on
hex_top and stagger.</t>
<t>Let (col, row) be the parsed column and row, adjusted so the grid-origin
hex is (0, 0). (The <tt>origin</tt> field controls which visual corner maps to
the lowest-numbered hex; the <tt>first</tt> field provides the starting number.
The conversion below assumes col and row have been zero-indexed by
subtracting <tt>first</tt>. The origin direction is handled separately as a
reflection — for <tt>&quot;bottom-left&quot;</tt> or <tt>&quot;bottom-right&quot;</tt>, negate the row
axis; for <tt>&quot;top-right&quot;</tt> or <tt>&quot;bottom-right&quot;</tt>, negate the column axis.)</t>
<t><strong>Flat-top, stagger: low (odd columns sit lower):</strong></t>

<artwork><![CDATA[u = col
w = row - floor(col / 2)
v = -u - w
]]></artwork>
<t><strong>Flat-top, stagger: high (odd columns sit higher):</strong></t>

<artwork><![CDATA[u = col
w = row - ceil(col / 2)
v = -u - w
]]></artwork>
<t><strong>Pointy-top, stagger: low (odd rows sit further right):</strong></t>

<artwork><![CDATA[u = col - floor(row / 2)
w = row
v = -u - w
]]></artwork>
<t><strong>Pointy-top, stagger: high (odd rows sit further left):</strong></t>

<artwork><![CDATA[u = col - ceil(row / 2)
w = row
v = -u - w
]]></artwork>
<t>PDS: can't we combine the two pairs of equations by introducing a stagger=0/1 parameter?</t>
</section>

<section anchor="neighbor-directions-in-cube-coordinates"><name>Neighbor directions in cube coordinates</name>
<t>The six neighbors of hex (u, v, w) and their edge directions:</t>
<t><strong>Flat-top:</strong></t>
<table>
<thead>
<tr>
<th>Edge</th>
<th>Delta u</th>
<th>Delta v</th>
<th>Delta w</th>
</tr>
</thead>

<tbody>
<tr>
<td>N</td>
<td>0</td>
<td>+1</td>
<td>-1</td>
</tr>

<tr>
<td>NE</td>
<td>+1</td>
<td>0</td>
<td>-1</td>
</tr>

<tr>
<td>SE</td>
<td>+1</td>
<td>-1</td>
<td>0</td>
</tr>

<tr>
<td>S</td>
<td>0</td>
<td>-1</td>
<td>+1</td>
</tr>

<tr>
<td>SW</td>
<td>-1</td>
<td>0</td>
<td>+1</td>
</tr>

<tr>
<td>NW</td>
<td>-1</td>
<td>+1</td>
<td>0</td>
</tr>
</tbody>
</table><t><strong>Pointy-top:</strong></t>
<table>
<thead>
<tr>
<th>Edge</th>
<th>Delta u</th>
<th>Delta v</th>
<th>Delta w</th>
</tr>
</thead>

<tbody>
<tr>
<td>NE</td>
<td>+1</td>
<td>0</td>
<td>-1</td>
</tr>

<tr>
<td>E</td>
<td>+1</td>
<td>-1</td>
<td>0</td>
</tr>

<tr>
<td>SE</td>
<td>0</td>
<td>-1</td>
<td>+1</td>
</tr>

<tr>
<td>SW</td>
<td>-1</td>
<td>0</td>
<td>+1</td>
</tr>

<tr>
<td>W</td>
<td>-1</td>
<td>+1</td>
<td>0</td>
</tr>

<tr>
<td>NW</td>
<td>0</td>
<td>+1</td>
<td>-1</td>
</tr>
</tbody>
</table></section>

<section anchor="hex-distance"><name>Hex distance</name>
<t>The distance between two hexes in cube coordinates is:</t>

<artwork><![CDATA[distance = max(|u1-u2|, |v1-v2|, |w1-w2|)
]]></artwork>
<t>Or equivalently:</t>

<artwork><![CDATA[distance = (|u1-u2| + |v1-v2| + |w1-w2|) / 2
]]></artwork>
</section>
</section>

<section anchor="serialization"><name>Serialization</name>

<section anchor="json-canonical"><name>JSON (canonical)</name>
<t>The canonical serialization is JSON (RFC 8259). A HexMap JSON file:</t>

<ul spacing="compact">
<li>MUST be valid JSON.</li>
<li>MUST be encoded as UTF-8.</li>
<li>SHOULD use the file extension <tt>.hexmap.json</tt>.</li>
<li>SHOULD use the media type <tt>application/vnd.hexerei.hexmap+json</tt>.</li>
</ul>
</section>

<section anchor="yaml-authoring-format"><name>YAML (authoring format)</name>
<t>YAML (version 1.2) is an equivalent serialization, recommended for
hand-authored maps. A HexMap YAML file:</t>

<ul spacing="compact">
<li>MUST produce a JSON-compatible data structure when parsed.
(YAML 1.2 is a superset of JSON; this is always true for documents
that avoid YAML-specific types like timestamps or binary.)</li>
<li>SHOULD use the file extension <tt>.hexmap.yaml</tt> or <tt>.hexmap.yml</tt>.</li>
<li>SHOULD use the media type <tt>application/vnd.hexerei.hexmap+yaml</tt>.</li>
<li>MAY contain comments (a key advantage over JSON for authoring).</li>
</ul>
<t>A HexMap YAML document, when converted to JSON, MUST validate against
the same JSON Schema as a native JSON document.</t>
</section>

<section anchor="validation"><name>Validation</name>
<t>Implementations SHOULD validate documents against the JSON Schema defined
in Section 10. Validation MUST be performed against the JSON representation
(converting from YAML first if necessary).</t>
</section>

<section anchor="round-trip-fidelity"><name>Round-trip fidelity</name>
<t>The format does not require byte-for-byte round-trip fidelity. An
implementation that reads and re-serializes a HexMap document MAY produce
different JSON/YAML (different whitespace, key ordering, feature
grouping) as long as the semantic content is preserved. See Section 4.7
for the definition of semantic equivalence.</t>
</section>
</section>

<section anchor="extensibility"><name>Extensibility</name>

<section anchor="the-extensions-object"><name>The <tt>extensions</tt> object</name>
<t>The top-level <tt>extensions</tt> object is reserved for implementation-specific
or game-specific data that falls outside this specification. Producers
MAY include arbitrary data here. Consumers MUST ignore extensions they
do not recognize.</t>

<sourcecode type="yaml"><![CDATA[extensions:
  hexerei_renderer:
    style: "classic-wargame"
    hex_size_px: 48
  my_game_engine:
    fog_of_war: true
    turn_limit: 12
]]></sourcecode>
</section>

<section anchor="the-properties-objects"><name>The <tt>properties</tt> objects</name>
<t>The <tt>properties</tt> field on terrain type definitions and on feature entries
is the primary mechanism for per-element extension data. Unrecognized
properties MUST be preserved and passed through.</t>
</section>

<section anchor="forward-compatibility"><name>Forward compatibility</name>
<t>Consumers that encounter unrecognized top-level keys SHOULD ignore them
(not reject the document). This allows future versions of the format to
add new sections without breaking older parsers.</t>
</section>
</section>

<section anchor="examples"><name>Examples</name>

<section anchor="minimal-map"><name>Minimal map</name>
<t>The smallest valid HexMap document:</t>
<t>{
  &quot;hexmap&quot;: &quot;1.0&quot;,
  &quot;grid&quot;: {
    &quot;hex_top&quot;: &quot;flat&quot;,
    &quot;columns&quot;: 3,
    &quot;rows&quot;: 3
  }
}</t>
<t>This defines a 3x3 flat-top hex grid with no terrain data.</t>
</section>

<section anchor="small-scenario-map-yaml"><name>Small scenario map (YAML)</name>
<t>A fragment inspired by Battle for Moscow, demonstrating most features:</t>
<t>hexmap: &quot;1.0&quot;</t>
<t>metadata:
  id: battle-for-moscow
  version: &quot;1.0&quot;
  title: &quot;Battle for Moscow&quot;
  designer: &quot;Frank Chadwick&quot;</t>
<t>grid:
  hex_top: flat
  columns: 22
  rows: 15
  stagger: low
  coordinates:
    label: XXYY
    origin: top-left
    first: [1, 1]
  geo:
    scale: 16000</t>
<t>terrain:
  hex:
    clear: {}
    forest: {}
    swamp: {}
    city: {}
    major_city: {}</t>
<t>edge:
    river: {}</t>
<t>path:
    road: {}
    railroad: {}</t>
<t>defaults:
  hex:
    terrain: clear
    elevation: 0</t>
<t>features:
  # === Terrain ===</t>

<ul>
<li><t>hexes: [&quot;0302&quot;, &quot;0303&quot;, &quot;0402&quot;]
terrain: forest</t>
</li>
<li><t>hex: &quot;0912&quot;
terrain: city
label: &quot;Kaluga&quot;</t>
</li>
<li><t>hex: &quot;1205&quot;
terrain: city
label: &quot;Tula&quot;
tags: [victory]</t>
</li>
<li><t>hex: &quot;1808&quot;
terrain: major_city
label: &quot;Moscow&quot;
tags: [victory]
properties:
  victory_points: 3</t>
</li>
</ul>
<t># === Rivers ===</t>

<ul spacing="compact">
<li>edges: [&quot;0910/NE&quot;, &quot;0910/N&quot;, &quot;0810/NE&quot;]
terrain: river</li>
</ul>
<t># === Roads and Railroads ===</t>

<ul>
<li><t>path: [&quot;1508&quot;, &quot;1608&quot;, &quot;1708&quot;, &quot;1808&quot;]
terrain: road
id: moscow-highway
label: &quot;Moscow Highway&quot;</t>
</li>
<li><t>path: [&quot;1205&quot;, &quot;1306&quot;, &quot;1407&quot;, &quot;1508&quot;, &quot;1608&quot;]
terrain: railroad
id: tula-rail</t>
</li>
</ul>
<t># === Regions ===</t>

<ul>
<li><t>region:
  id: soviet-fortifications
  hexes: [&quot;0805&quot;, &quot;0905&quot;, &quot;1005&quot;, &quot;1105&quot;]
tags: [fortified]
label: &quot;Soviet Fortification Line&quot;</t>
</li>
<li><t>region:
  id: german-entry-west
  hexes: [&quot;0101&quot;, &quot;0102&quot;, &quot;0103&quot;, &quot;0104&quot;, &quot;0105&quot;,
          &quot;0106&quot;, &quot;0107&quot;, &quot;0108&quot;, &quot;0109&quot;, &quot;0110&quot;]
tags: [entry]
label: &quot;German Entry (West Edge)&quot;</t>
</li>
</ul>
</section>

<section anchor="tactical-scale-map-fragment-yaml"><name>Tactical-scale map fragment (YAML)</name>
<t>Panzer Blitz-style tactical features with elevation and slopes:</t>
<t>hexmap: &quot;1.0&quot;</t>
<t>metadata:
  id: tactical-demo
  title: &quot;Tactical Demo - Hill 231&quot;</t>
<t>grid:
  hex_top: flat
  columns: 10
  rows: 8
  geo:
    scale: 250</t>
<t>terrain:
  hex:
    clear: {}
    woods: {}
    town: {}
    rough: {}
  edge:
    slope:
      directed: true
    stream: {}
    wall:
      name: &quot;Stone Wall&quot;
  vertex:
    bridge: {}
  path:
    road: {}</t>
<t>defaults:
  hex:
    terrain: clear
    elevation: 0</t>
<t>features:
  # Wooded hills
  - hexes: [&quot;0303&quot;, &quot;0304&quot;]
    terrain: woods
    elevation: 1</t>
<t># The hilltop
  - hex: &quot;0403&quot;
    terrain: rough
    elevation: 2
    label: &quot;Hill 231&quot;</t>

<ul spacing="compact">
<li>hex: &quot;0404&quot;
terrain: rough
elevation: 1</li>
</ul>
<t># Town
  - hex: &quot;0504&quot;
    terrain: town
    label: &quot;Bergdorf&quot;</t>
<t># Slope hexsides around the hill (directed: 0403 is the &quot;active&quot; side)
  - edges: [&quot;0403/N&quot;, &quot;0403/NE&quot;, &quot;0403/SE&quot;,
            &quot;0403/S&quot;, &quot;0403/SW&quot;, &quot;0403/NW&quot;]
    terrain: slope</t>
<t># Stream south of town
  - edge_path: [&quot;0505/SW&quot;, &quot;0505/S&quot;, &quot;0605/SW&quot;]
    terrain: stream
    label: &quot;Bergbach&quot;</t>
<t># Stone walls around town
  - edges: [&quot;0504/NW&quot;, &quot;0504/NE&quot;]
    terrain: wall</t>
<t># Bridge over the stream
  - vertex: &quot;0505.SW&quot;
    terrain: bridge
    label: &quot;Stone Bridge&quot;</t>
<t># Road through the valley
  - path: [&quot;0104&quot;, &quot;0204&quot;, &quot;0304&quot;, &quot;0404&quot;, &quot;0504&quot;, &quot;0604&quot;]
    terrain: road
    id: main-road</t>
</section>
</section>

<section anchor="json-schema"><name>JSON Schema</name>
<t>The normative JSON Schema for the HexMap format is maintained as a
separate file: <tt>hexmap.schema.json</tt>. Below is a detailed outline of the
schema structure and key constraints.</t>

<artwork><![CDATA[HexMapDocument
├── hexmap: string (const: "1.0") [REQUIRED]
├── metadata: MetadataObject
│   ├── id: string (pattern: "^[a-z][a-z0-9-]*$")
│   ├── version: string
│   ├── title, description, designer, publisher, date: string
│   └── source: { url: string (format: uri), notes: string }
├── grid: GridObject [REQUIRED]
│   ├── hex_top: enum ["flat", "pointy"] [REQUIRED]
│   ├── columns: integer (minimum: 1) [REQUIRED]
│   ├── rows: integer (minimum: 1) [REQUIRED]
│   ├── stagger: enum ["low", "high"]
│   ├── boundary: array of string (minItems: 1)
│   ├── coordinates: CoordinatesObject
│   │   ├── label: string (default: "XXYY")
│   │   ├── origin: enum ["top-left", "bottom-left",
│   │   │                  "top-right", "bottom-right"]
│   │   └── first: array [integer, integer] (default: [1, 1])
│   └── geo: GeoObject
│       ├── scale: number (exclusiveMinimum: 0)
│       ├── anchor: array [number, number]
│       ├── anchor_hex: string
│       ├── bearing: number (minimum: 0, exclusiveMaximum: 360)
│       └── projection: string (default: "mercator")
├── terrain: TerrainVocabulary
│   ├── hex: map<string, TerrainTypeDef>
│   ├── edge: map<string, TerrainTypeDef>
│   ├── vertex: map<string, TerrainTypeDef>
│   └── path: map<string, TerrainTypeDef>
│   where TerrainTypeDef:
│       ├── name: string
│       ├── directed: boolean (only for edge subsection)
│       ├── style: { color, pattern, stroke, stroke_width }
│       └── properties: object
├── defaults: DefaultsObject
│   ├── hex: { terrain: string, elevation: integer, ... }
│   ├── edge: { terrain: string, ... }
│   └── vertex: { terrain: string, ... }
├── features: array of FeatureEntry
│   └── FeatureEntry: oneOf
│       ├── HexFeature: { hex: string | hexes: [string], ... }
│       ├── EdgeFeature: { edge: string | edges: [string], ... }
│       ├── VertexFeature: { vertex: string | vertices: [string], ... }
│       ├── PathFeature: { path: [string] | edge_path: [string], ... }
│       └── RegionFeature: { region: { id, hexes }, ... }
│   common attributes:
│       ├── terrain: string
│       ├── elevation: integer
│       ├── label: string
│       ├── id: string
│       ├── tags: array of string
│       └── properties: object
└── extensions: object (additionalProperties: true)
]]></artwork>
<t><strong>Key constraints:</strong></t>

<ul spacing="compact">
<li>Terrain type identifiers MUST match the pattern <tt>^[a-z][a-z0-9_]*$</tt>.</li>
<li>Feature <tt>terrain</tt> values MUST reference a type defined in the
appropriate terrain vocabulary subsection.</li>
<li>Edge references MUST match the pattern
<tt>^.+/(N|NE|E|SE|S|SW|W|NW|[1-6])$</tt>.</li>
<li>Vertex references MUST match the pattern
<tt>^.+\.(N|NE|E|SE|S|SW|W|NW|[1-6])$</tt>.</li>
<li>Path arrays MUST have at least 2 elements.</li>
<li>Region <tt>hexes</tt> arrays MUST have at least 1 element.</li>
</ul>
<t>The JSON Schema file is the authoritative reference for validation
details including all required/optional field distinctions, type
constraints, and enum values.</t>
</section>

<section anchor="test-cases"><name>Test Cases</name>
<t>The following real wargame maps are identified as test cases for format
validation. Each exercises different aspects of the format. Reference
encodings will be maintained in the <tt>tests/</tt> directory.</t>
<t><strong>Acceptance criteria</strong> for each test case: the reference HexMap file
must (a) validate against the JSON Schema, (b) round-trip through a
JSON&lt;-&gt;YAML conversion with semantic equivalence, and (c) produce the
correct graph structure (hex count, edge adjacencies, path connectivity).</t>

<section anchor="battle-for-moscow-frank-chadwick-gdw-1986"><name>Battle for Moscow (Frank Chadwick / GDW, 1986)</name>
<t><strong>Exercises:</strong> Basic operational map, XXYY coordinates, terrain (clear,
forest, swamp, city, major city), rivers on edges, roads and railroads
as paths, fortification regions, victory hexes.</t>
<t><strong>Scale:</strong> 16 km/hex, 22x15 grid, flat-top.</t>
<t><strong>Complexity:</strong> Low. Ideal first complete encoding.</t>
</section>

<section anchor="panzer-blitz-panzer-leader-avalon-hill-1970-1974"><name>Panzer Blitz / Panzer Leader (Avalon Hill, 1970/1974)</name>
<t><strong>Exercises:</strong> Tactical scale, elevation levels (0-2), slope hexsides
(directed edges), woods/town/rough terrain, roads, streams on edges.
Geomorphic map boards (multiple boards per scenario).</t>
<t><strong>Scale:</strong> 250m/hex, 4 boards of ~16x10 each, flat-top.</t>
<t><strong>Complexity:</strong> Medium. Tests elevation, directed edges.
Multi-board composition deferred to v2 (see Appendix D, Q1).</t>
</section>

<section anchor="advanced-squad-leader-boards-mmp"><name>Advanced Squad Leader boards (MMP)</name>
<t><strong>Exercises:</strong> Maximum terrain complexity. Multi-story buildings (stone
vs wood), orchards, grain fields, brush, gullies, bridges, fords,
walls, hedges, bocage, railroad embankments. Elevation 0-3+.</t>
<t><strong>Scale:</strong> 40m/hex, various board sizes, pointy-top (unusual).</t>
<t><strong>Complexity:</strong> High. Stress test for terrain vocabulary and edge types.</t>
</section>

<section anchor="the-russian-campaign-jedko-ah-1974"><name>The Russian Campaign (Jedko/AH, 1974)</name>
<t><strong>Exercises:</strong> Large operational map, weather zones, sea hexes, straits,
complex rail network, Dnepr River as long connected edge path.</t>
<t><strong>Scale:</strong> ~50 km/hex, large map (~34x22), flat-top.</t>
<t><strong>Complexity:</strong> Medium-high. Tests large maps and long paths.</t>
</section>

<section anchor="drive-on-metz-spi-1980"><name>Drive on Metz (SPI, 1980)</name>
<t><strong>Exercises:</strong> Mid-complexity operational map, fortification hexes,
river crossings, bridge vertices, road network, urban terrain.</t>
<t><strong>Scale:</strong> ~2 km/hex.</t>
<t><strong>Complexity:</strong> Medium.</t>
</section>

<section anchor="hold-the-line-worthington-2008"><name>Hold the Line (Worthington, 2008)</name>
<t><strong>Exercises:</strong> AWI-era map, simpler terrain vocabulary, elevation
(hills), woods, towns, stone walls on edges. Geomorphic boards.</t>
<t><strong>Scale:</strong> Abstract/tactical.</t>
<t><strong>Complexity:</strong> Low-medium. Good test of edge features (walls).</t>
</section>

<section anchor="synthetic-procedural-test-maps"><name>Synthetic / procedural test maps</name>
<t><strong>Exercises:</strong> Programmatic generation. A 5x5 minimal map for unit
testing. A large 100x100 map for performance. Irregular boundaries.
Maps with every feature type.</t>
</section>
</section>

<section anchor="security-considerations"><name>Security Considerations</name>
<t>HexMap documents are data files with no executable content. Parsers
SHOULD NOT evaluate any string field as code. Standard JSON/YAML parsing
security considerations apply: parsers SHOULD reject excessively large
documents, deeply nested structures, and (for YAML) MUST disable
arbitrary object instantiation (i.e., use safe loading).</t>
<t>File paths and URLs in <tt>metadata.source</tt> are informational and MUST NOT
be automatically fetched or executed.</t>
</section>

<section anchor="references"><name>References</name>

<section anchor="normative"><name>Normative</name>

<ul spacing="compact">
<li>RFC 8259: The JavaScript Object Notation (JSON) Data Interchange Format</li>
<li>RFC 2119: Key words for use in RFCs to Indicate Requirement Levels</li>
<li>YAML 1.2 Specification: <eref target="https://yaml.org/spec/1.2.2/">https://yaml.org/spec/1.2.2/</eref></li>
<li>JSON Schema (2020-12): <eref target="https://json-schema.org/specification">https://json-schema.org/specification</eref></li>
</ul>
</section>

<section anchor="informative"><name>Informative</name>

<ul spacing="compact">
<li>Red Blob Games, Hexagonal Grids:
<eref target="https://www.redblobgames.com/grids/hexagons/">https://www.redblobgames.com/grids/hexagons/</eref></li>
<li>Amit Patel, &quot;Implementation of Hex Grids&quot; (coordinate systems, offset
schemes, conversions)</li>
<li>VASSAL Engine Terrain Recording:
<eref target="https://vassalengine.org/wiki/Terrain_Recording">https://vassalengine.org/wiki/Terrain_Recording</eref></li>
<li>Worldographer file format:
<eref target="https://worldographer.com/instructions/file-format/">https://worldographer.com/instructions/file-format/</eref></li>
<li>LaTeX wargame package:
<eref target="https://vladar.bearblog.dev/latex-wargame-package/">https://vladar.bearblog.dev/latex-wargame-package/</eref></li>
</ul>
</section>
</section>
</section>

</middle>

<back>

<section anchor="appendix-a-conventional-terrain-vocabulary-non-normative"><name>Appendix A: Conventional Terrain Vocabulary (Non-Normative)</name>
<t>For WWII-era hex wargames, the following terrain type identifiers are
RECOMMENDED as a starting point for interoperability. Maps are not
required to use these exact identifiers.</t>

<section anchor="hex-terrain-types"><name>Hex terrain types</name>
<table>
<thead>
<tr>
<th>Identifier</th>
<th>Name</th>
<th>Typical games</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>clear</tt></td>
<td>Clear / Open</td>
<td>All</td>
</tr>

<tr>
<td><tt>forest</tt></td>
<td>Forest / Woods</td>
<td>All</td>
</tr>

<tr>
<td><tt>light_woods</tt></td>
<td>Light Woods / Orchard</td>
<td>Tactical</td>
</tr>

<tr>
<td><tt>rough</tt></td>
<td>Rough / Broken</td>
<td>Many</td>
</tr>

<tr>
<td><tt>swamp</tt></td>
<td>Swamp / Marsh</td>
<td>Many</td>
</tr>

<tr>
<td><tt>mountain</tt></td>
<td>Mountain</td>
<td>Operational+</td>
</tr>

<tr>
<td><tt>desert</tt></td>
<td>Desert</td>
<td>North Africa</td>
</tr>

<tr>
<td><tt>city</tt></td>
<td>City / Town</td>
<td>All</td>
</tr>

<tr>
<td><tt>major_city</tt></td>
<td>Major City</td>
<td>Operational+</td>
</tr>

<tr>
<td><tt>village</tt></td>
<td>Village</td>
<td>Tactical</td>
</tr>

<tr>
<td><tt>water</tt></td>
<td>Water / Lake</td>
<td>Many</td>
</tr>

<tr>
<td><tt>ocean</tt></td>
<td>Ocean / Sea</td>
<td>Naval, coastal</td>
</tr>

<tr>
<td><tt>farmland</tt></td>
<td>Farmland / Fields</td>
<td>Tactical</td>
</tr>

<tr>
<td><tt>bocage</tt></td>
<td>Bocage</td>
<td>Normandy</td>
</tr>

<tr>
<td><tt>major_river</tt></td>
<td>Major River (hex-filling)</td>
<td>Operational+</td>
</tr>
</tbody>
</table></section>

<section anchor="edge-terrain-types"><name>Edge terrain types</name>
<table>
<thead>
<tr>
<th>Identifier</th>
<th>Name</th>
<th>Directed?</th>
<th>Typical games</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>river</tt></td>
<td>River</td>
<td>no</td>
<td>Most</td>
</tr>

<tr>
<td><tt>major_river</tt></td>
<td>Major / Wide River</td>
<td>no</td>
<td>Operational+</td>
</tr>

<tr>
<td><tt>stream</tt></td>
<td>Stream / Creek</td>
<td>no</td>
<td>Tactical</td>
</tr>

<tr>
<td><tt>cliff</tt></td>
<td>Cliff / Escarpment</td>
<td><strong>yes</strong></td>
<td>Many</td>
</tr>

<tr>
<td><tt>slope</tt></td>
<td>Slope / Elevation Change</td>
<td><strong>yes</strong></td>
<td>Tactical</td>
</tr>

<tr>
<td><tt>wall</tt></td>
<td>Wall (stone, etc.)</td>
<td>no</td>
<td>Tactical</td>
</tr>

<tr>
<td><tt>hedge</tt></td>
<td>Hedge</td>
<td>no</td>
<td>Tactical</td>
</tr>

<tr>
<td><tt>bocage</tt></td>
<td>Bocage (edge)</td>
<td>no</td>
<td>Normandy</td>
</tr>

<tr>
<td><tt>ford</tt></td>
<td>Ford</td>
<td>no</td>
<td>Many</td>
</tr>

<tr>
<td><tt>impassable</tt></td>
<td>Impassable Hexside</td>
<td>no</td>
<td>Various</td>
</tr>
</tbody>
</table></section>

<section anchor="vertex-terrain-types"><name>Vertex terrain types</name>
<table>
<thead>
<tr>
<th>Identifier</th>
<th>Name</th>
<th>Typical games</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>bridge</tt></td>
<td>Bridge</td>
<td>Many</td>
</tr>

<tr>
<td><tt>ford</tt></td>
<td>Ford</td>
<td>Many</td>
</tr>

<tr>
<td><tt>crossroads</tt></td>
<td>Crossroads</td>
<td>Tactical</td>
</tr>
</tbody>
</table></section>

<section anchor="path-terrain-types"><name>Path terrain types</name>
<table>
<thead>
<tr>
<th>Identifier</th>
<th>Name</th>
<th>Typical games</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>road</tt></td>
<td>Road (primary)</td>
<td>Most</td>
</tr>

<tr>
<td><tt>secondary_road</tt></td>
<td>Secondary Road / Track</td>
<td>Many</td>
</tr>

<tr>
<td><tt>trail</tt></td>
<td>Trail</td>
<td>Tactical</td>
</tr>

<tr>
<td><tt>railroad</tt></td>
<td>Railroad</td>
<td>Operational+</td>
</tr>

<tr>
<td><tt>river</tt></td>
<td>River (connected course)</td>
<td>Most</td>
</tr>
</tbody>
</table></section>
</section>

<section anchor="appendix-b-clock-direction-system"><name>Appendix B: Clock Direction System</name>
<t>PDS: we should make more of this. &quot;secret weapon&quot; for making the format machine-friendly?
Perhaps MUST support for implementations, even if the user-facing docs emphasize compass directions. It makes rotation and reflection algorithms much easier to write.  how
to clarify indexed (1-6) vs hours (1-12)?</t>
<t>The clock direction system provides a unified, orientation-independent
way to reference the 12 geometric features around a hex (6 edges and
6 vertices), numbered 1-12 clockwise from 12 o'clock (straight up).</t>
<t>Edges and vertices alternate around the hex. Which positions are edges
and which are vertices depends on <tt>hex_top</tt>:</t>

<section anchor="flat-top-edges-at-even-hours-vertices-at-odd-hours"><name>Flat-top (edges at even hours, vertices at odd hours)</name>

<artwork><![CDATA[                  11       12        1
                   ·________________·
                  / (NW v)  (N e)    \
         10     /    (NE v)           \     2
        (NW e) /                       \ (NE e)
              /                         \
          9  ·                           ·  3
        (W v)\                           /(E v)
              \                         /
         8     \                       /     4
        (SW e)  \                     / (SE e)
                 \___________________/
                   ·                 ·
                  7        6         5
               (SW v)   (S e)     (SE v)
]]></artwork>
<t>PDS: can we add a pointy version side-by-side with above?  also the NW/N/NE labels should be outside not inside</t>
<table>
<thead>
<tr>
<th>Clock</th>
<th>Feature</th>
<th>Compass</th>
<th>Index</th>
</tr>
</thead>

<tbody>
<tr>
<td>12</td>
<td>edge</td>
<td>N</td>
<td>/1</td>
</tr>

<tr>
<td>1</td>
<td>vertex</td>
<td>NE</td>
<td>.1</td>
</tr>

<tr>
<td>2</td>
<td>edge</td>
<td>NE</td>
<td>/2</td>
</tr>

<tr>
<td>3</td>
<td>vertex</td>
<td>E</td>
<td>.2</td>
</tr>

<tr>
<td>4</td>
<td>edge</td>
<td>SE</td>
<td>/3</td>
</tr>

<tr>
<td>5</td>
<td>vertex</td>
<td>SE</td>
<td>.3</td>
</tr>

<tr>
<td>6</td>
<td>edge</td>
<td>S</td>
<td>/4</td>
</tr>

<tr>
<td>7</td>
<td>vertex</td>
<td>SW</td>
<td>.4</td>
</tr>

<tr>
<td>8</td>
<td>edge</td>
<td>SW</td>
<td>/5</td>
</tr>

<tr>
<td>9</td>
<td>vertex</td>
<td>W</td>
<td>.5</td>
</tr>

<tr>
<td>10</td>
<td>edge</td>
<td>NW</td>
<td>/6</td>
</tr>

<tr>
<td>11</td>
<td>vertex</td>
<td>NW</td>
<td>.6</td>
</tr>
</tbody>
</table></section>

<section anchor="pointy-top-vertices-at-even-hours-edges-at-odd-hours"><name>Pointy-top (vertices at even hours, edges at odd hours)</name>
<table>
<thead>
<tr>
<th>Clock</th>
<th>Feature</th>
<th>Compass</th>
<th>Index</th>
</tr>
</thead>

<tbody>
<tr>
<td>12</td>
<td>vertex</td>
<td>N</td>
<td>.1</td>
</tr>

<tr>
<td>1</td>
<td>edge</td>
<td>NE</td>
<td>/1</td>
</tr>

<tr>
<td>2</td>
<td>vertex</td>
<td>NE</td>
<td>.2</td>
</tr>

<tr>
<td>3</td>
<td>edge</td>
<td>E</td>
<td>/2</td>
</tr>

<tr>
<td>4</td>
<td>vertex</td>
<td>SE</td>
<td>.3</td>
</tr>

<tr>
<td>5</td>
<td>edge</td>
<td>SE</td>
<td>/3</td>
</tr>

<tr>
<td>6</td>
<td>vertex</td>
<td>S</td>
<td>.4</td>
</tr>

<tr>
<td>7</td>
<td>edge</td>
<td>SW</td>
<td>/4</td>
</tr>

<tr>
<td>8</td>
<td>vertex</td>
<td>SW</td>
<td>.5</td>
</tr>

<tr>
<td>9</td>
<td>edge</td>
<td>W</td>
<td>/5</td>
</tr>

<tr>
<td>10</td>
<td>vertex</td>
<td>NW</td>
<td>.6</td>
</tr>

<tr>
<td>11</td>
<td>edge</td>
<td>NW</td>
<td>/6</td>
</tr>
</tbody>
</table><t>The pattern: for flat-top, edges are at even clock positions (12, 2, 4,
6, 8, 10) and vertices at odd (1, 3, 5, 7, 9, 11). For pointy-top,
it's reversed. Mnemonic: <strong>even hours hit flat sides</strong>.</t>
<t>This system is informative — compass directions remain the primary
addressing mechanism. But clock positions are useful for algorithms and
for compact notation in geometry expressions (Appendix C).</t>
</section>
</section>

<section anchor="appendix-c-geometry-expressions-future"><name>Appendix C: Geometry Expressions (Future)</name>
<t><strong>This appendix is NOT part of the v1.0 format.</strong> It sketches a design
direction for a compact expression language in future versions. Nothing
in this appendix is normative or implemented. It is preserved here to
record design thinking and invite feedback.</t>
<t>PDS: feels like there's a happy median where we extend the current
structured features using arrays, nested arrays etc.  maybe that looks a bit
like more like geojson tho i don't know that we need all the formalism of rings and holes etc?</t>
<t>don't implement full Boolean set operations, that's more a geometry engine.
rely on stencilling layers, e.g. set all as swamp, then reset a hex as island</t>
<t>each feature resolves to a HexCollection, EdgeCollection or VertexCollection
for hex paths we want connectivity info, but that could be an edge collection.</t>
<t>implicit conversion, e.g. HexCollection can be converted to all edges it contains
(excluding half edge boundary?) so hex path =&gt; interior edges for river/road crossings</t>
<t>or maybe need explicit conversion like inf/sup?</t>
<t>operators:
  border(collection) returns the subset of collection which have neighbors not in collection
  filled(collection) returns superset of collection adding items within the the boundary
    of adjacent neighbors (e.g. for vertices within the edge boundary)</t>
<t>boundary (sided?)
within/inside
span/incident/closure = boundary + within</t>
<t>hexes =&gt; having_all(edges)
  hexes =&gt; having_any(edges)</t>
<t>path: [start, end], closed?, nudge?
features:
  - { hex: a23, id: moscow }
  - { hex: [ #moscow, #leningrad, z13 ], id: cities }
  - { hex: #some-edges }
  - { hexes: }
  - { hexpath: [ b13, '...', e13] } # resolves to edge collection, equiv to edges: hexes: [ ... ]
  hexes:</t>
<t>Gemini ideas:</t>

<artwork><![CDATA[features:
  - hexes:
      rect: ["0101", "1010"] # Selects a bounding box
      exclude: ["0505"]      # Set subtraction
    terrain: forest

  - edge_path:
      from: "0101"
      to: "0501"
      nudge: "N"             # Resolves the "tie-break" shortest path problem
    terrain: road

  # Selects all edges that form the perimeter of a hex-set
- edges:
    boundary_of:
      region: "soviet-fortifications"
    side: "internal"  # Targets the half-edges belonging to the hexes inside
  terrain: wall

features:
  - region:
      id: soviet-fortifications
      # Instead of listing every hex, define a span
      span: ["0805..1105"]  # or explicit operator ["0805", "...", "1105"] ?
    tags: [fortified]
    label: "Soviet Fortification Line"

  # this seems a bit forced
features:
  - edge_path:
      # Trace defines the "river bed" by listing the hexes it flows between
      trace: ["0910", "1010", "1009", "1109"]
      # 'side' resolves which specific hexside is used when moving between hexes
      mode: "between"
    terrain: river
    label: "Nara River"

features:
  - edges:
      boundary_of: { hex: "0403" }
      # 'directed: true' in your terrain vocabulary (Section 4.5)
      # is satisfied here by the selector implicitly targeting the half-edges of 0403.
    terrain: slope

- path:
    from: "0101"
    to: "0505"
    nudge: "NE" # Prefers the most North-Eastern route if multiple exist
  terrain: railroad

features:
  - id: "soviet_zone"
    hexes: { rect: ["1010", "2020"] }
    # This entry just defines a region, no terrain yet

  - name: "Fortified Line"
    edges: { boundary_of: { ref: "soviet_zone" } }
    terrain: trench
]]></artwork>

<section anchor="motivation"><name>Motivation</name>
<t>Hand-authoring large maps requires compact ways to specify collections
of hexes, edges, and vertices. Listing every hex in a forest or every
edge in a river is tedious and error-prone.</t>
</section>

<section anchor="proposed-notation"><name>Proposed notation</name>
<t><strong>Hex list:</strong></t>

<artwork><![CDATA[A13, A25, B16
]]></artwork>
<t><strong>Hex path (shortest path between two hexes):</strong></t>

<artwork><![CDATA[A13 - A17
]]></artwork>
<t>Valid only when there is a unique shortest path between the endpoints.</t>
<t><strong>Hex loop (closed path):</strong></t>

<artwork><![CDATA[A13 - A17 - C17 - C13 -
]]></artwork>
<t>Trailing <tt>-</tt> closes the loop back to the first hex.</t>
<t><strong>Hex region (filled area from boundary path):</strong></t>

<artwork><![CDATA[A13 - A17 - C17 - C13 -*
]]></artwork>
<t><tt>-*</tt> means &quot;close the loop and fill the interior.&quot;</t>
<t><strong>Edge path:</strong></t>

<artwork><![CDATA[A13/N - A17/N
]]></artwork>
<t>Connected sequence of edges.</t>
<t><strong>Set operations:</strong></t>

<artwork><![CDATA[region1, region2          # union
region1 & region2         # intersection
region1 - region2         # difference
]]></artwork>
<t><strong>Geometry conversion:</strong></t>

<artwork><![CDATA[/forest-region            # edges within a hex region
.forest-region            # vertices within a hex region
|forest-region            # boundary edges of a hex region
]]></artwork>
</section>

<section anchor="half-edge-boundaries"><name>Half-edge boundaries</name>
<t>The boundary of a hex region naturally produces half-edges (each boundary
edge belongs to a hex inside the region). The <tt>|</tt> operator could return
these as directed edge references, useful for marking the boundary of a
terrain region automatically.</t>
</section>

<section anchor="open-questions"><name>Open questions</name>

<ul spacing="compact">
<li>Tie-breaking when multiple shortest paths exist (nudge direction?)</li>
<li>How to specify non-convex regions</li>
<li>Whether expressions can reference named features by id</li>
<li>Performance implications for very large selections</li>
</ul>
</section>
</section>

<section anchor="appendix-d-open-questions"><name>Appendix D: Open Questions</name>
<t>Issues to resolve in future revisions:</t>

<ol spacing="compact">
<li><strong>Multi-board / geomorphic maps.</strong> Panzer Blitz and ASL use
interchangeable map boards composed into different configurations per
scenario. The format needs a way to define individual boards and
compose them — including rotation and offset of coordinates. This
could be a <tt>boards</tt> section that lists sub-maps with transforms, or
a composition document that references separate HexMap files.
<strong>v1.0 encodes single boards only.</strong> Multi-board composition is
deferred to a future version. The <tt>tile</tt> terminology is reserved
for this use.</li>
</ol>
<t>PDS: we should solve in v1.  Most of the map setup (metadata, terrain, grid) would be shared,
perhaps just allow a list of mapboard: { id: defaults: features: extensions: }  ?</t>

<ol spacing="compact" start="2">
<li><strong>Terrain stacking.</strong> Can a hex have multiple terrain types? (e.g.,
forest + hill, or road passing through a town). Current model: a
hex has one <tt>terrain</tt> type plus <tt>elevation</tt> plus <tt>tags</tt>. The layered
features model allows later entries to override, but not stack.
Should <tt>terrain</tt> be an array? Or is elevation + tags + properties
sufficient to capture stacking?</li>
</ol>
<t>PDS: perhaps distinguish a set of base terrains that are mutually
exclusive (last one applied wins) from 'modifier' terrains which
can stack.  e.g. one of forrest, swamp or plains; but several of road, river, fort?</t>

<ol start="3">
<li><t><strong>Road junctions.</strong> Some roads fork inside a hex (entering via one
edge, exiting via two). The path model assumes linear sequences.
Solutions: allow branching paths, or decompose into multiple paths
sharing a waypoint.
PDS: decomposition seems fine</t>
</li>
<li><t><strong>Partial hexes.</strong> Real maps have partial hexes at boundaries. Should
these be explicitly marked, or inferred from the boundary?
PDS: seems more like a rendering concern, unless they have different play
mechanics?  ie. are they regular hexes that are just clipped at the boundary,
are the excluded from the map (invalid) or somehow special?</t>
</li>
<li><t><strong>River/road intersection semantics.</strong> When a road path crosses a
river edge, is there an implicit bridge? Or must bridges be
explicitly annotated as vertex features?
PDS: typically a road would be thru hexes, and river along edges,
with bridge as edge terrain.  we shouldn't build any semantics for
how terrains interact.</t>
</li>
<li><t><strong>Stagger naming.</strong> Is <tt>&quot;low&quot;</tt> / <tt>&quot;high&quot;</tt> intuitive enough? Other
candidates: <tt>&quot;indent-first&quot;</tt> / <tt>&quot;indent-second&quot;</tt>, or describe the
visual pattern with a small diagram in the spec. (Diagrams are now
included in Section 4.3; the naming question remains open.)
PDS: ya, i don't think it's intuitive, which should brainstorm alternatives</t>
</li>
<li><t><strong>Coordinate label patterns.</strong> The <tt>XXYY</tt> / <tt>AY</tt> pattern system is
simple but limited. Does it cover all real-world wargame labeling
schemes? What about systems like &quot;hex 3-1204&quot; (board 3, hex 1204)?
Multi-board notation is deferred to v2.
PDS: let's collect a set of real-world examples and review.  i
think there might also be different alpha counting schems,
e.g. A .. Z, AA, BB, CC, ... vs A .. Z, AA, AB, AC ...
eg. excel style base-26 vs repeating A .., AA ..., AAA ...
in general could use an invertible u/v/w &lt;=&gt; label function but don't really want that complexity</t>
</li>
<li><t><strong>Compact edge notation.</strong> Rivers with many edges are still verbose
even with <tt>edge_path</tt>. The geometry expression language (Appendix C)
addresses this for v2.
PDS: yes we should solve for a version of appendix C</t>
</li>
<li><t><strong>Feature ordering guarantees.</strong> The spec says &quot;later entries override.&quot;
Should implementations also support explicit priority/z-order for
cases where document order isn't the desired precedence?
PDS: worth exploring, but maybe back-references and labeled feature groups would be enough?
seems like there is lots of prior art for how to do stuff like that in other structured doc types</t>
</li>
</ol>
</section>

</back>

</rfc>
